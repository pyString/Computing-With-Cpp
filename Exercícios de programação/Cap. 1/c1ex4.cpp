/*
Escreva um programa que calcula o produto dos primeiros dez inteiros positivos, 
1 × 2 × ... × 10, e a soma de seus inversos 1/1 + 1/2 + ... + 1/10. 
Isso é mais difícil do que parece. Primeiro, você deve saber que o símbolo *, e
não um ×, é usado para multiplicação em C++. Tente escrever o programa,
e confira os resultados usando uma calculadora. Os resultados do programa
não serão exatamente corretos. Então escreva os números como números
em ponto flutuante, 1.0, 2.0,..., 10.0, e execute novamente o programa.
Você pode explicar a diferença dos resultados? Vamos explicar este fenômeno no Capítulo 2.
*/

#include <iostream>

using namespace std;

int main()
{
    return 0;
}