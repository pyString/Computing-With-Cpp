/**
 * Nomes de arquivos e extensões. Escreva um programa que solicita ao usuário
 * a letra que indica o dispositivo (C), o caminho (\Windows\System), 
 * o nome do arquivo (Readme) e a extensão (TXT). 
 * Após imprime o nome completo do arquivo C:\Windows\System\Readme.TXT (se você usa UNIX
 * ou um Macintosh, use / ou : para separar diretórios).
 */

#include <iostream>

int main()
{
    return 0;
}